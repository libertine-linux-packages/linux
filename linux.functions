# This file is part of libertine linux's package linux. It is subject to the license terms in the COPYRIGHT file found in the top-level directory of this distribution and at https://raw.githubusercontent.com/libertine-linux-packages/linux/master/COPYRIGHT. No part of libertine linux's package linux, including this file, may be copied, modified, propagated, or distributed except according to the terms contained in the COPYRIGHT file.
# Copyright © 2016 The developers of libertine linux's package linux. See the COPYRIGHT file in the top-level directory of this distribution and at https://raw.githubusercontent.com/libertine-linux-packages/linux/master/COPYRIGHT.


case "$package" in
	
	build_musl_cross_make_linux)
		
		variant=empty

		depends build_busybox
				
		copy_subset="$variant"
		
		libertine_compile_build_musl_cross_make_linux()
		{
			:
		}
		
	;;
	
	linux_prep|linux)
	
		variant=upstream
		
		depends build_gnumake
		build_needs make

		depends build_ccache
		build_needs ccache
		
		depends build_bash
		build_needs bash
		
		depends build_netbsd_src_patch
		build_needs patch
		
		depends build_perl
		build_needs perl
		
		depends build_musl_cross_make_build
		build_needs "$libertine_build"-cpp "$libertine_build"-cc "$libertine_build"-gcc "$libertine_build"-c++ "$libertine_build"-g++ "$libertine_build"-ar "$libertine_build"-ld "$libertine_build"-ranlib
		
		depends build_musl_cross_make_host
		build_needs "$libertine_host"-cpp "$libertine_host"-cc "$libertine_host"-gcc "$libertine_host"-ar "$libertine_host"-ld "$libertine_host"-ranlib "$libertine_host"-strip "$libertine_host"-nm "$libertine_host"-objcopy "$libertine_host"-objdump "$libertine_host"-readelf
		
		depends build_bc
		build_needs bc
		
		depends build_busybox
		build_needs cmp tail bzip2 od cut touch xargs echo sort uniq mktemp dirname basename install sed gzip date head fgrep readlink expr wc uname seq diff
		# seq is needed by grsecurity's generate_size_overflow_hash.sh
		
		# SPL / ZFS
		if core_variable_isUnset linux_prep_enableZfs; then
			linux_prep_enableZfs='true'
		fi
		if $linux_prep_enableZfs; then
			depends zlib libuuid libtirpc libblkid
			
			depends build_paxctl
			build_needs paxctl

			depends build_slibtool
			build_needs clibtool-static libtoolize realpath

			depends build_perl
			build_needs perl

			depends build_autoconf
			build_needs autoconf autoheader autom4te autoreconf autoscan autoupdate ifnames

			depends build_automake
			build_needs automake aclocal

			depends build_gnum4
			build_needs m4
		fi
		
		# Needs the 'full' xz, not BusyBox's, for scripts/gen_initramfs_list.sh
		depends build_xz_utils_xz
		build_needs xz
		build_needs which
		
		depends build_libressl
		depends build_zlib

		_libertine_compile_linux_menuconfigAdjustments()
		{
			local configFilePath="$1"
			local initramFsGenerateFilePath="$2"
	
			libertine_public_menuconfig_adjust CONFIG_INITRAMFS_SOURCE "$initramFsGenerateFilePath" true "$configFilePath"
		}

		build_needs sed
		_libertine_compile_linux_make()
		{
			local makeFolderPath="$1"
			shift 1
	
			pushd "$makeFolderPath"
	
				libertine_public_make \
					V=2 \
					ARCH="$libertine_hostLinuxSpecific" \
					KBUILD_BUILD_TIMESTAMP="$libertine_hostUnameTimestamp" \
					KBUILD_BUILD_USER='libertine' \
					KBUILD_BUILD_HOST='libertine' \
					CROSS_COMPILE="$libertine_host"- \
					HOSTCFLAGS="-Wall -Wmissing-prototypes -Wstrict-prototypes -O0 -fomit-frame-pointer -std=gnu89 -I$(libertine_public_outputBuildSysrootPath build_zlib)/usr/include -L$(libertine_public_outputBuildSysrootPath build_zlib)/usr/lib -I$(libertine_public_outputBuildSysrootPath build_libressl)/usr/include -L$(libertine_public_outputBuildSysrootPath build_libressl)/usr/lib -lz" \
					"$@"
		
			popd
		}
	;;
	
esac

case "$package" in
		
	linux_prep)
				
		build_needs rm ln
		libertine_compile_linux_prep()
		{
			# Apply patches
			"$(libertine_public_packagePath)"/linux-patches/apply-patches "$(libertine_public_sourcePath)"
			
			local configFilePath="$(libertine_public_sourcePath)"/.config
			
			printf '' >"$(libertine_public_outputInitramfsContentsFilePath)"
			_libertine_compile_linux_menuconfigAdjustments "$configFilePath" "$(libertine_public_outputInitramfsContentsFilePath)"
			
			_libertine_compile_linux_prep_fixGenInitramfsListSh
			
			_libertine_compile_linux_prep_menuconfigAlterationsBeforeNonKernelSuppliedModules "$configFilePath"
			
			# Fix-up gcc plugin compilation for cross-compiling
			_libertine_compile_linux_prep_fixGccPluginsForCrossCompilation
			
			# Fix-up grsecurity Makefile assuming absolute paths
			_libertine_compile_linux_prep_fixGrsecurityAbsolutePaths
			
			# Linux's Makefile and other tools assume /bin/bash
			_libertine_compile_linux_prep_fixHardcodedBashReferencesInMakefileAndElsewhere "$(libertine_public_sourcePath)"
			
			# Linux's Makefile hardcodes gcc and g++ rather than cc and c++
			_libertine_compile_linux_prep_fixHardcodedGccReferencesInMakefile
			
			# grsecurity relies on include/generated/autoconf.h. Running this make task should create it
			_libertine_compile_linux_make "$(libertine_public_sourcePath)" silentoldconfig
			
			# Prepare for, and then build, extra modules
			_libertine_compile_linux_make "$(libertine_public_sourcePath)" prepare scripts
			
			if $linux_prep_enableZfs; then
				_libertine_compile_linux_prep_nonKernelSuppliedModule_buildSplAndZfs
			fi
			
			_libertine_compile_linux_make "$(libertine_public_sourcePath)"
	
			rm -rf "$(libertine_public_outputMiscPath)"
			ln -s copy/"$variant" "$(libertine_public_outputMiscPath)"
		}
		
		_libertine_compile_linux_prep_fixGenInitramfsListSh_xzArguments()
		{
			# out-of-memory!
			# xzArguments='--lzma2=preset=9e,dict=1536MiB,lc=3,lp=1,pb=0,mode=normal,nice=273,mf=bt4,depth=0'
			xzArguments='--lzma2=preset=9e,dict=64MiB,lc=3,lp=1,pb=0,mode=normal,nice=273,mf=bt4,depth=0'

			# ia64, armthumb and sparc not supported
			case "$libertine_host" in
				
				i?86*|x86_64*)
					if $libertine_hostIsBigEndian; then
						:
					else
						xzArguments="--x86=start=0 $xzArguments"
					fi
				;;
				
				# Big Endian only
				powerpc*)
					if $libertine_hostIsBigEndian; then
						xzArguments="--powerpc=start=0 $xzArguments"
					fi
				;;
				
				# Little Endian only
				arm*)
					if $libertine_hostIsBigEndian; then
						:
					else
						xzArguments="--arm=start=0 $xzArguments"
					fi
				;;
				
			esac
			
			xzArguments="-T 0 --check=crc32 $xzArguments"
		}

		build_needs sed
		_libertine_compile_linux_prep_fixGenInitramfsListSh()
		{
			local xzArguments
			_libertine_compile_linux_prep_fixGenInitramfsListSh_xzArguments
			
			sed -i \
				-e 's;my_mtime=$(find "\$1" -printf "%T@\\n" | sort -r | head -n 1);my_mtime='"$libertine_hostUnameTimestampSecondsSince1970"';g' \
				-e 's/-printf "%p %m %U %G\\n"/| xargs stat -c "%n %a %u %g"/g' \
				-e 's;xz --check=crc32 --lzma2=dict=1MiB;xz '"$xzArguments"';g' \
				-e 's;\$(date -d"\$KBUILD_.*;";g' \
				-e 's;usr/gen_init_cpio \$timestamp ;usr/gen_init_cpio -t '"$libertine_hostUnameTimestampSecondsSince1970"' ;g' \
				"$(libertine_public_sourcePath)"/scripts/gen_initramfs_list.sh
		}
		
		_libertine_compile_linux_prep_nonKernelSuppliedModule_buildSplAndZfs()
		{
			if [ -e "$(libertine_public_sourcePath)"/grsecurity ]; then
				printf '\n\n\nZFS on Linux 4.4 does not compile with grsec currently (Jan 20 2017)\n\n\n' 1>&2
				return 0
			fi
			
			# We do this as SPL and ZFS both look for a hardcoded 'cpp'
			libertine_public_addOrReplaceBinaryInPath cpp "$libertine_build"-cpp

			_libertine_compile_linux_prep_nonKernelSuppliedModule_spl

			_libertine_compile_linux_prep_nonKernelSuppliedModule_zfs

			_libertine_compile_linux_prep_menuconfigAlterationsForZfsAfterNonKernelSuppliedModules "$configFilePath"
		}

		build_needs find xargs sed
		_libertine_compile_linux_prep_fixHardcodedBashReferencesInMakefileAndElsewhere()
		{
			local path="$1"
			
			local buildBashPath="$(libertine_public_buildNeedsPath)"/bash
			
			libertine_public_adjustShaBangAllFilesInPathEvenIfNotFirstLine "$path"
			
			find "$path" -type f -name 'Makefile*' | xargs sed -i 's;/bin/bash;'"$buildBashPath"';g'
		}
		
		_libertine_compile_linux_prep_menuconfigAlterationsBeforeNonKernelSuppliedModules()
		{
			local configFilePath="$1"
	
			libertine_public_menuconfig_adjust CONFIG_CROSS_COMPILE "$libertine_host"- true "$configFilePath"
			libertine_public_menuconfig_adjust CONFIG_INITRAMFS_ROOT_UID "$libertine_public_uid" false "$configFilePath"
			libertine_public_menuconfig_adjust CONFIG_INITRAMFS_ROOT_GID "$libertine_public_gid" false "$configFilePath"
			# CONFIG_EXTRA_FIRMWARE="intel-ucode/06-3a-09 amd-ucode/microcode_amd_fam15h.bin"
			libertine_public_menuconfig_adjust CONFIG_EXTRA_FIRMWARE_DIR "$(libertine_public_packagePath)"/linux-firmware true "$configFilePath"
		}
		
		build_needs sed cat
		_libertine_compile_linux_prep_fixGccPluginsForCrossCompilation()
		{
			# Note this does not fix the problem that system.h (in plugin/include) requires gmp.h or libiberty.h creating a conflicting basename because gcc's auto-host.h is incorrectly generated
			cat <<-'EOF' >"$(libertine_public_sourcePath)"/scripts/gcc-plugin.sh
				echo "$3"
			EOF
			
			# This is just plain weird - GCC Plugins needs gmp but use the plugin includes of the target.
			# Add -I, -L for gmp, mpc and mpfr
			local buildFolderPath="$(libertine_public_packagePath build_musl_cross_make_build)"/upstream/build
			local objToolchainFolderPath="$buildFolderPath"/obj_toolchain

			local gmpIncludePath="$objToolchainFolderPath"/gmp
			local gmpLibraryPath="$objToolchainFolderPath"/gmp/.libs
			
			local mpcIncludePath="$buildFolderPath"/src_mpc/src
			local mpcLibraryPath="$objToolchainFolderPath"/.libs
			
			local mpfrIncludePath="$buildFolderPath"/src_mpfr/src
			local mpfrLibraryPath="$objToolchainFolderPath"/mpfr/src/.libs
			
			sed -i \
				-e 's,HOST_EXTRACXXFLAGS += -I,HOST_EXTRACXXFLAGS += -I'"$gmpIncludePath"' -L'"$gmpLibraryPath"' -I'"$mpcIncludePath"' -L'"$mpcLibraryPath"' -I'"$mpfrIncludePath"' -L'"$mpfrLibraryPath"' -I,g' \
				"$(libertine_public_sourcePath)"/scripts/gcc-plugins/Makefile
			
			sed -i \
				-e 's/-shared/-shared -static-libstdc++/g' \
				"$(libertine_public_sourcePath)"/scripts/Makefile.host
		}
		
		build_needs sed
		_libertine_compile_linux_prep_fixGrsecurityAbsolutePaths()
		{
			if [ -e "$(libertine_public_sourcePath)"/grsecurity ]; then
				sed -i \
					-e '/\/boot/d' \
					-e '/\/lib/d' \
					"$(libertine_public_sourcePath)"/grsecurity/Makefile
			fi
		}
		
		build_needs sed
		_libertine_compile_linux_prep_fixHardcodedGccReferencesInMakefile()
		{
			# (1) HOSTCC is hardcoded to gcc
			# (2) HOSTCXX is hardcoded to g++
			# (3) CC is hardcoded to $(CROSS_COMPILE)gcc
			sed -i \
				-e 's/^HOSTCC[[:space:]]*= gcc/HOSTCC = '"${libertine_build}"'-cc/g' \
				-e 's/^HOSTCXX[[:space:]]*= g++/HOSTCXX = '"${libertine_build}"'-c++/g' \
				-e 's/$(CROSS_COMPILE)gcc/$(CROSS_COMPILE)cc/g' \
				"$(libertine_public_sourcePath)"/Makefile
		}

		build_needs sed
		_libertine_compile_linux_prep_nonKernelSuppliedModule_fixesForThingsThatAssumeGnuCoreutilsAndNotBusyBox()
		{
			# Not an exact match, but good enough
			sed -i -e 's/--canonicalize-existing/-f/g' copy-builtin
			
			# Naive
			sed -i -e 's/--recursive/-r/g' copy-builtin
		}
		
		build_needs sed rm cp
		_libertine_compile_linux_prep_nonKernelSuppliedModule_spl()
		{
			pushd "$(libertine_public_packagePath)"/spl_upstream
				
				_libertine_compile_linux_prep_nonKernelSuppliedModule_fixesForThingsThatAssumeGnuCoreutilsAndNotBusyBox
				
				_libertine_compile_linux_prep_fixHardcodedBashReferencesInMakefileAndElsewhere "$(libertine_public_packagePath)"/spl_upstream
				
				# Remove stuff that (a) requires fixes and (b) slows down the build
				local tab=$"\t"
				sed -i -e 's/SUBDIRS = include rpm/SUBDIRS = include/g' -e "s/SUBDIRS += lib cmd man scripts/SUBDIRS += lib scripts/g" -e "/rpm.am/d" -e "/deb.am/d" -e "/tgz.am/d" Makefile.am
				sed -i -e "/${tab}cmd/d" -e "/${tab}man/d" -e "/${tab}rpm/d" configure.ac
				
				sed -i -e 's/AC_PROG_LIBTOOL/LT_INIT/g' configure.ac
				libtoolize --install --force --verbose
				cp -a -f -H m4/. config/
				autoreconf --install --force --verbose || true
				rm -Rf autom4te.cache
				cp -a -f -H build-aux/. config/
				
				libertine_public_autoconf_removeCFlags='-ffunction-sections -fdata-sections -Wl,--gc-sections' libertine_public_autoconf_modernAutoconfConfigure \
					--disable-debug-kmem \
					--disable-debug-kmem-tracking \
					--enable-linux-builtin=yes \
					--with-linux="$(libertine_public_sourcePath)" \
					--with-linux-obj="$(libertine_public_sourcePath)"
				
				#typedef ulong_t				pfn_t;
				sed -i -e '/typedef.*pfn_t;/d' include/sys/types.h
				
				# Requires bash. No really.
				bash ./copy-builtin "$(libertine_public_sourcePath)"
				
				libertine_public_make
		
				libertine_public_make DESTDIR="$(libertine_public_outputHostSysrootPath)" install
		
			popd
		}
		
		build_needs sed truncate mktemp dd
		_libertine_compile_linux_prep_nonKernelSuppliedModule_zfs()
		{
			libertine_public_populateInputSysroot zlib libuuid libtirpc libblkid
	
			pushd "$(libertine_public_packagePath)"/zfs_upstream
			
				_libertine_compile_linux_prep_nonKernelSuppliedModule_fixesForThingsThatAssumeGnuCoreutilsAndNotBusyBox
				
				_libertine_compile_linux_prep_fixHardcodedBashReferencesInMakefileAndElsewhere "$(libertine_public_packagePath)"/zfs_upstream
			
				# Remove stuff that (a) requires even more build binaries and (b) slows down the build
				local tab=$"\t"
				sed -i -e "s/SUBDIRS += udev etc man scripts tests lib cmd contrib/SUBDIRS += scripts lib cmd/g" -e "/rpm.am/d" -e "/deb.am/d" -e "/tgz.am/d" Makefile.am
				sed -i -e "/${tab}udev/d" -e "/${tab}etc/d" -e "/${tab}man/d" -e "/${tab}contrib/d" -e "/${tab}tests/d" -e "/${tab}rpm/d" configure.ac
				
				sed -i -e 's/AC_PROG_LIBTOOL/LT_INIT/g' configure.ac
				libtoolize --install --force --verbose
				cp -a -f -H m4/. config/
				autoreconf --install --force --verbose || true
				rm -Rf autom4te.cache
				cp -a -f -H build-aux/. config/
				
				libertine_public_autoconf_removeCFlags='-ffunction-sections -fdata-sections -Wl,--gc-sections -fno-common' libertine_public_autoconf_modernAutoconfConfigure \
					--disable-debug-dmu-tx \
				    --enable-linux-builtin=yes \
				    --with-linux="$(libertine_public_sourcePath)" \
				    --with-linux-obj="$(libertine_public_sourcePath)" \
				    --with-spl="$(libertine_public_packagePath)"/spl_upstream \
				    --with-spl-obj="$(libertine_public_packagePath)"/spl_upstream \
					--enable-sysvinit \
					--with-mounthelperdir=/usr/sbin \
					--with-blkid=yes
				
				# zed_log uses read, close, pipe, getpid and relies on glibc header behaviour
				sed -i -e 's/#include "zed_log.h"/#include <unistd.h>\n#include "zed_log.h"/g' cmd/zed/zed_log.c
				
				# Requires bash. No really.
				bash ./copy-builtin "$(libertine_public_sourcePath)"
		
				libertine_public_make
		
				libertine_public_make DESTDIR="$(libertine_public_outputHostSysrootPath)" install
	
			popd
			
			libertine_public_installAndStripBinaryIn sbin mount.zfs
			libertine_public_installAndStripBinaryIn sbin zdb
			libertine_public_installAndStripBinaryIn sbin zed
			libertine_public_installAndStripBinaryIn sbin zfs
			libertine_public_installAndStripBinaryIn sbin zhack
			libertine_public_installAndStripBinaryIn sbin zinject
			libertine_public_installAndStripBinaryIn sbin zpios
			libertine_public_installAndStripBinaryIn sbin zpool
			libertine_public_installAndStripBinaryIn sbin zstreamdump
			libertine_public_installAndStripBinaryIn sbin ztest
		}

		_libertine_compile_linux_prep_menuconfigAlterationsForZfsAfterNonKernelSuppliedModules()
		{
			libertine_public_menuconfig_adjust CONFIG_SPL y false "$configFilePath"
			libertine_public_menuconfig_adjust CONFIG_ZFS y false "$configFilePath"
		}
	;;
	
	linux)
		
		copy_subset="empty"
		
		depends linux_prep
		
		depends build_rsync
		build_needs rsync
		
		depends build_mlb
		build_needs mlbinstall
		
		depends build_dosfstools
		build_needs mkfs.fat
		
		depends build_gptfdisk
		build_needs sgdisk
		
		depends build_netbsd_src_pax
		build_needs pax

		depends build_libressl
		build_needs openssl
		
		depends ipxe
		
		build_needs touch
		libertine_compile_linux()
		{
			local inputPath="$(libertine_public_inputMakePath)"
			local initramfsContentsFolderPath="$inputPath"/initramfs
			local outputInitramFsGenerateFilePath="$inputPath"/initramfs.contents
			local configFilePath="$inputPath"/.config
			
			_libertine_compile_linux_copyLinuxPrepOutputAndRemoveUnwantedInitramfsStuff
			
			_libertine_compile_linux_menuconfigAdjustments "$configFilePath" "$outputInitramFsGenerateFilePath"
			_libertine_compile_linux_menuconfigAlterationsForHostName "$configFilePath"
			_libertine_compile_linux_menuconfigAlterationsForCmdline "$configFilePath"
			
			_libertine_compile_linux_initramfs_createCpioArchiveInputFile "$outputInitramFsGenerateFilePath"
			
			_libertine_compile_linux_make "$inputPath"
			
			_libertine_compile_linux_replaceInstallShForIncreasedReproducibility
			
			_libertine_compile_linux_make "$inputPath" INSTALL_PATH="$(libertine_public_outputMiscPath)" install
			
			libertine_public_touchWithKnownDateTime "$(libertine_public_outputMiscPath)"/vmlinuz
			
			_libertine_compile_linux_copyInitramfs
			
			_libertine_compile_linux_createEfiSystemPartition
			
			_libertine_compile_linux_createChainloadedIpxeBootScript
			
			_libertine_compile_linux_signLinux
		}
		
		build_needs mkdir rm
		_libertine_compile_linux_copyLinuxPrepOutputAndRemoveUnwantedInitramfsStuff()
		{
			mkdir -m 0755 -p "$inputPath"
			libertine_public_copy "$(libertine_public_outputMiscPath linux_prep)"/. "$(libertine_public_inputMakePath)"/
			
			# Remove linux_prep inputs which aren't wanted, so make will rebuild
			set +f
				# Usually, the file is initramfs_data.cpio.gz
				rm -rf "$inputPath"/usr/initramfs_data.cpio* "$inputPath"/usr/initramfs_data.o "$inputPath"/usr/built-in.o
			set -f

			rm -rf "$initramfsContentsFolderPath"
			mkdir -m 0700 -p "$initramfsContentsFolderPath"
			
			rm -rf "$outputInitramFsGenerateFilePath"
		}
		
		build_needs sed rm
		_libertine_compile_linux_menuconfigAlterationsForHostName()
		{
			local configFilePath="$1"
			
			libertine_public_menuconfig_adjust CONFIG_DEFAULT_HOSTNAME "$libertine_machine" true "$configFilePath"
			
			pushd "$(libertine_public_inputMakePath)"
				
				sed -i -e 's;#define UTS_DOMAINNAME "(none)";#define UTS_DOMAINNAME "'"$machine_networkingDomain"'";g' include/linux/uts.h
				
				rm -rf \
					arch/x86/boot/compressed/aslr.o \
					drivers/usb/gadget/legacy/inode.o \
					init/version.o \
					kernel/utsname.o \
					kernel/utsname_sysctl.o
			popd
		}
		
		build_needs head
		_libertine_compile_linux_menuconfigAlterationsForCmdline()
		{
			local configFilePath="$1"
						
			# Iterate over packages, finding the value of any command-line settings
			local actualCommandLine=''
			_libertine_compile_linux_menuconfigAlterationsForCmdline_processPackageCommandLine()
			{
				local dependentPackage="$1"
				shift 1
			
				local kernelBootCommandLineFilePath="$(libertine_public_outputKernelBootCommandLineFilePath "$dependentPackage")"

				if [ ! -s "$kernelBootCommandLineFilePath" ]; then
					return 0
				fi
				
				local value="$(head -n 1 "$kernelBootCommandLineFilePath")"
				if [ -z "$value" ]; then
					return 0
				fi
				
				if [ -z "$actualCommandLine" ]; then
					actualCommandLine="$value"
				else
					actualCommandLine="$actualCommandLine $value"
				fi
			}
			libertine_public_iterateOverAllProductionPackagesInReverseDependencyOrder 'linux' _libertine_compile_linux_menuconfigAlterationsForCmdline_processPackageCommandLine
			
			if core_variable_isUnsetOrEmpty linux_postpendToBuiltInCommandLine; then
				:
			elif [ -z "$actualCommandLine" ]; then
				actualCommandLine="$linux_postpendToBuiltInCommandLine"
			else
				actualCommandLine="$actualCommandLine $linux_postpendToBuiltInCommandLine"
			fi
			
			if core_variable_isUnset linux_forceUseOfBuiltInCommandLine; then
				linux_forceUseOfBuiltInCommandLine='false'
			fi

			if [ -n "$actualCommandLine" ]; then
				libertine_public_menuconfig_adjust CONFIG_CMDLINE_BOOL y false "$configFilePath"
				libertine_public_menuconfig_adjust CONFIG_CMDLINE "$actualCommandLine" true "$configFilePath"
				if $linux_forceUseOfBuiltInCommandLine; then
					libertine_public_menuconfig_adjust CONFIG_CMDLINE_OVERRIDE y false "$configFilePath"
				else
					libertine_public_menuconfig_unset CONFIG_CMDLINE_OVERRIDE "$configFilePath"
				fi
			else
				if $linux_forceUseOfBuiltInCommandLine; then
					libertine_public_menuconfig_adjust CONFIG_CMDLINE_BOOL y false "$configFilePath"
					libertine_public_menuconfig_adjust CONFIG_CMDLINE '' true "$configFilePath"
					libertine_public_menuconfig_adjust CONFIG_CMDLINE_OVERRIDE y false "$configFilePath"
				fi
			fi
		}
		
		build_needs ln rm mkdir sed tac sort find cat chmod xargs touch
		_libertine_compile_linux_initramfs_createCpioArchiveInputFile()
		{
			_libertine_compile_linux_initramfs_generateEssentialPerMachineNetworkingFiles
			
			_libertine_compile_linux_initramfs_installMachineFilesystemAsIfItWereOurOwn
			
			
			
			local cpioOutputParentFolderPath="$(libertine_public_outputMiscPath)"/initramfs
			local cpioOutputRootFolderPath="$cpioOutputParentFolderPath"/root
			mkdir -m 0755 -p "$cpioOutputRootFolderPath"
			
			
			# Combine initramfs sysroots and give all files and folder the same timestamp
			libertine_public_iterateOverAllProductionPackagesInReverseDependencyOrder 'linux' _libertine_compile_linux_initramfs_mergeInitramfsSysroots 1>&2
			chmod 0755 "$cpioOutputRootFolderPath"  # Forces what will become '/' to have correct permissions after rsync merge
			find . -depth -print0 | xargs -0 touch -c -h -t "$libertine_hostUnameTimestampTouch"
			
			
			local generatedPackagesFilePath="$cpioOutputParentFolderPath"/000-generated.initramfs.contents
			_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfs
			{
				pushd "$cpioOutputRootFolderPath"
			
					# Setting IFS to . allows us to strip a leading '.' from paths from find
					# Using -mindepth 1 eliminates './'
					local empty
					local path
					find . -mindepth 1 -depth | while IFS='.' read -r empty path
					do
						local fileEtcPath=."$path"
						
						if [ -L "$fileEtcPath" ]; then
							printf 'slink %s %s %s 0 0\n' "$path" "$(_libertine_compile_linux_initramfs_symbolicLinkValue "$fileEtcPath")" "$(_libertine_compile_linux_initramfs_octalMode "$fileEtcPath")"
						elif [ -f "$fileEtcPath" ]; then
							printf 'file %s %s %s 0 0\n' "$path" initramfs"${path}" "$(_libertine_compile_linux_initramfs_octalMode "$fileEtcPath")"
						elif [ -d "$fileEtcPath" ]; then
							printf 'dir %s %s 0 0\n' "$path" "$(_libertine_compile_linux_initramfs_octalMode "$fileEtcPath")"
						elif [ -b "$fileEtcPath" ]; then
							printf 'nod %s %s 0 0 b %s\n' "$path" "$(_libertine_compile_linux_initramfs_octalMode "$fileEtcPath")" "$(_libertine_compile_linux_initramfs_deviceMajorAndMinor "$fileEtcPath")"
						elif [ -c "$fileEtcPath" ]; then
							printf 'nod %s %s 0 0 c %s\n' "$path" "$(_libertine_compile_linux_initramfs_octalMode "$fileEtcPath")" "$(_libertine_compile_linux_initramfs_deviceMajorAndMinor "$fileEtcPath")"
						elif [ -p "$fileEtcPath" ]; then
							printf 'pipe %s %s 0 0\n' "$path" "$(_libertine_compile_linux_initramfs_octalMode "$fileEtcPath")"
						elif [ -S "$fileEtcPath" ]; then
							core_exitError $core_commandLine_exitCode_CONFIG "Sockets are not acceptable in a rootfs ram disk"
						else
							core_exitError $core_commandLine_exitCode_UNAVAILABLE "What is ${fileEtcPath}?"
						fi
					done
					
				popd
			} >"$generatedPackagesFilePath"
			rm -rf $initramfsContentsFolderPath
			ln -s "$cpioOutputRootFolderPath" $initramfsContentsFolderPath
			
			
						
			
			
			local mergedFilePath="$cpioOutputParentFolderPath"/001-merged.initramfs.contents
			libertine_public_iterateOverAllProductionPackagesInReverseDependencyOrder 'linux' _libertine_compile_linux_initramfs_mergeInitramfsContentsWhilstRemovingEmptyLinesAndComments

			local allFilePath="$cpioOutputParentFolderPath"/002-all.initramfs.contents
			cat "$generatedPackagesFilePath" "$mergedFilePath" >"$allFilePath"

			local laterPackagesTrumpEarlierPackagesFilePath="$cpioOutputParentFolderPath"/003-later-packages-trump-earlier-packages.initramfs.contents
			tac "$allFilePath" >"$laterPackagesTrumpEarlierPackagesFilePath"
			
			local deduplicatedFilePath="$cpioOutputParentFolderPath"/004-deduplicated.initramfs.contents
			sort -u -k2,2 "$laterPackagesTrumpEarlierPackagesFilePath" >"$deduplicatedFilePath"
			
			local sortedSoDirAreFirstFilePath="$cpioOutputParentFolderPath"/005-sorted-so-dir-are-first.initramfs.contents
			sort "$deduplicatedFilePath" >"$sortedSoDirAreFirstFilePath"
			
			rm -rf "$outputInitramFsGenerateFilePath"
			ln -s "$sortedSoDirAreFirstFilePath" "$outputInitramFsGenerateFilePath"
		}
		
		build_needs mkdir
		_libertine_compile_linux_initramfs_generateEssentialPerMachineNetworkingFiles()
		{
			local etcFolderPath="$(libertine_public_outputInitramfsPath)"/etc
			mkdir -m 0755 -p "$etcFolderPath"
			
			# Create /etc/hostname
			printf '%s\n' "$libertine_machine" >"$etcFolderPath"/hostname
			
			# Create /etc/dnsdomainname
			printf '%s\n' "$machine_networkingDomain" >"$etcFolderPath"/dnsdomainname
			
			# Create /etc/resolv.conf
			{
				printf 'domain %s\n' "$machine_networkingResolvConfDomain"
				
				IFS=' ' set -- $machine_networkingIpv4DomainNameServers $machine_networkingIpv6DomainNameServers
				local nameserver
				for nameserver in "$@"
				do
					printf 'nameserver %s\n' "$nameserver"
				done
				
				printf 'options rotate\n'
				printf 'options attempt:5\n'
				printf 'options timeout:2\n'
				# Prefer IPv6 AAAA queries, and wrap A query answers as IPv4 in IPv6 addresses
				#printf 'options inet6\n'
				#printf 'options edns0\n'
		
			} >"$etcFolderPath"/resolv.conf
		}
		
		build_needs cp
		_libertine_compile_linux_initramfs_installMachineFilesystemAsIfItWereOurOwn()
		{
			if [ -d "$libertine_machineSettingsFolderPath"/initramfs ]; then
				libertine_public_copy_filesystem "$libertine_machineSettingsFolderPath"/initramfs/ "$(libertine_public_outputInitramfsPath)"/
			fi
			
			local contentsFilePath="$libertine_machineSettingsFolderPath"/initramfs.contents
			if [ -f "$contentsFilePath" ]; then
				cp -f "$contentsFilePath" "$(libertine_public_outputInitramfsContentsFilePath)"
			fi
		}

		build_needs rsync
		_libertine_compile_linux_initramfs_mergeInitramfsSysroots()
		{
			local package="$1"
			shift 1
			
			printf '\nRSYNC PACKAGE TO CPIO: %s\n' "$package"
			rsync --archive --itemize-changes --inplace --super --whole-file --outbuf=Line "$(libertine_public_outputInitramfsPath "$package")"/ "$cpioOutputRootFolderPath"/
			printf 'RSYNC-ED PACKAGE TO CPIO: %s\n' "$package"
		}
		
		build_needs sed
		_libertine_compile_linux_initramfs_mergeInitramfsContentsWhilstRemovingEmptyLinesAndComments()
		{
			local package="$1"
			shift 1
			
			sed \
				-e '/^#/d' \
				-e '/^\s*$/d' \
				"$(libertine_public_outputInitramfsContentsFilePath "$package")" \
				>>"$mergedFilePath"
		}
		
		build_needs rm
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfs()
		{
			local combineFilePath
			set +f
			for combineFilePath in "$cpioOutputRootFolderPath"/etc/combine.d/*.combine
			do
				set -f
				
				printf '%s\n' "Combining file '$combineFilePath'" 1>&2
				
				local parentFolderPath fileName permissions mergeStrategy
				while IFS=' ' read -r parentFolderPath fileName permissions mergeStrategy
				do	
					case "$parentFolderPath" in
						
						/*)
							:
						;;
						
						*)
							printf '%s\n' "A relative parentFolderPath '$parentFolderPath' is not allowed in the .combine file '$combineFilePath'" 1>&2
							exit 1
						;;
						
					esac
					
					local combinedFolderPath="$cpioOutputRootFolderPath""$parentFolderPath"/"$fileName".d
					local combinedFilePath="$cpioOutputRootFolderPath""$parentFolderPath"/"$fileName"

					printf '\t%s\n' "Combining to create '$combinedFilePath' using mergeStrategy '$mergeStrategy'" 1>&2
					
					local deleteWhenFinished=true
					case "$mergeStrategy" in
		
						nocombine)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategy_nocombine
							deleteWhenFinished=false
						;;
		
						group)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategy_group
						;;
		
						passwd)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost passwd
						;;
		
						shadow)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost shadow
						;;
		
						default)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost default
						;;
		
						hasfiles)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost hasfiles
						;;
		
						nosort)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost nosort
						;;
		
						interfaces)
							_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost interfaces
						;;
		
						*)
							core_exitError 1 "Unknown mergeStrategy '$mergeStrategy' for $combinedFolderPath"
						;;
		
					esac
					
					if $deleteWhenFinished; then
						
						# Permissions may be messy...
						chmod -R 0755 "$combinedFolderPath"; rm -rf "$combinedFolderPath"
					fi
					
				done <"$combineFilePath"
				
			done
			set -f
			
			chmod -R 0755 "$cpioOutputRootFolderPath"/etc/combine.d; rm -rf "$cpioOutputRootFolderPath"/etc/combine.d
		}
		
		build_needs chmod
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategy_nocombine()
		{
			local individualFilePath
			set +f
			for individualFilePath in "$combinedFolderPath"/*."$fileName"
			do
				set -f
				
				if [ ! -f "$individualFilePath" ]; then
					continue
				fi
	
				chmod "$permissions" "$individualFilePath"
			done
			set -f
		}
		
		build_needs chmod head sort cp
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategy_group()
		{
			local TMP_FILE
			core_temporaryFiles_newFileToRemoveOnExit
			local temporaryFilePath="$TMP_FILE"
	
			printf '' >"$temporaryFilePath"
			
			local groupFilePath
			set +f
			for groupFilePath in "$combinedFolderPath"/*
			do
				set -f
		
				local groupWithoutExtensionPath="${groupFilePath%*.${fileName}.d}"
				local groupName="${groupWithoutExtensionPath##*/}"
				local groupId="$(head -n 1 "$groupFilePath"/id)"
					
				local users=''
				local userFilePath
				set +f
				for userFilePath in "$groupFilePath"/*.user
				do
					set -f
					
					if [ ! -e "$userFilePath" ]; then
						continue
					fi
							
					local userWithoutExtensionPath="${userFilePath%*.user}"
					local userName="${userWithoutExtensionPath##*/}"
			
					if [ -n "$users" ]; then
						users="${users},"
					fi
					users="${users}${userName}"
				done
				set -f
		
				printf '%s:x:%s:%s\n' "$groupName" "$groupId" "$users" >>"$temporaryFilePath"
			done

			# Create file
			sort -u -n -t : -k 3 "$temporaryFilePath" >"$combinedFilePath"
	
			# Create backup file
			local backupFilePath="$combinedFilePath"-
			cp -f "$combinedFilePath" "$backupFilePath"
			chmod "$permissions" "$backupFilePath"
	
			# Finish
			chmod "$permissions" "$combinedFilePath"
		}
		
		build_needs chmod sed rm
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost()
		{
			local actionToTake="$1"
			
			local TMP_FILE
			core_temporaryFiles_newFileToRemoveOnExit
			local temporaryFilePath="$TMP_FILE"
	
			printf '' >"$temporaryFilePath"
	
			local hasFiles=false
			local individualFilePath
			set +f
			for individualFilePath in "$combinedFolderPath"/*."$fileName"
			do
				set -f
				if [ ! -f "$individualFilePath" ]; then
					continue
				fi
		
				chmod "$permissions" "$individualFilePath"
		
				# Remove comments and empty lines and append to a temporary file
				sed -e '/^#/d' -e '/^\s*$/d' "$individualFilePath" >>"$temporaryFilePath"
				hasFiles=true
			done
			set -f
			
			# Template
			local TMP_FILE
			core_temporaryFiles_newFileToRemoveOnExit
			local heredocWrapperFilePath="$TMP_FILE"
			
			{
				printf 'cat >"$combinedFilePath" <<EOF\n'
				cat "$temporaryFilePath"
				printf '\nEOF\n'
			} >"$heredocWrapperFilePath"
			. "$heredocWrapperFilePath"
			
			_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost_action_$actionToTake
			
			if [ -e "$combinedFilePath" ]; then
				chmod "$permissions" "$combinedFilePath"
			fi
			rm -rf "$temporaryFilePath"
		}
		
		build_needs sort cp chmod
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost_action_passwd()
		{			
			# Create file
			sort -u -n -t : -k 3 "$temporaryFilePath" >"$combinedFilePath"
	
			# Create backup file
			local backupFilePath="$combinedFilePath"-
			cp -f "$combinedFilePath" "$backupFilePath"
			chmod "$permissions" "$backupFilePath"
		}
		
		build_needs sort cp chmod
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost_action_shadow()
		{
			# Create file
			sort -u "$temporaryFilePath" >"$combinedFilePath"
	
			# Create backup file
			local backupFilePath="$combinedFilePath"-
			cp -f "$combinedFilePath" "$backupFilePath"
			chmod "$permissions" "$backupFilePath"
		}
		
		build_needs sort
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost_action_default()
		{
			# We should really normalise whitespace and merge lines before doing this
			# Hard task to merge IPv6 lines as they have multiple naming choices
			sort -u "$temporaryFilePath" >"$combinedFilePath"
		}
		
		build_needs sort
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost_action_hasfiles()
		{
			if $hasFiles; then
				sort -u "$temporaryFilePath" >"$combinedFilePath"
			fi
		}

		build_needs cp
		_libertine_compile_linux_initramfs_combineSnippetsAndRemoveFromInitramfsmergeStrategyForMost_action_nosort()
		{
			cp "$temporaryFilePath" "$combinedFilePath"
		}
		
		build_needs readlink
		_libertine_compile_linux_initramfs_symbolicLinkValue()
		{
			local path="$1"
			
			readlink "$path"
		}

		build_needs stat
		_libertine_compile_linux_initramfs_octalMode()
		{
			local path="$1"
	
			if [ -L "$path" ]; then
				printf '0755'
				return 0
			fi
	
			local octalMode="$(stat -c '%a' "$path")"
			if [ ${#octalMode} -eq 3 ]; then
				octalMode='0'"${octalMode}"
			fi
	
			printf '%s' "$octalMode"
		}

		build_needs stat
		_libertine_compile_linux_initramfs_deviceMajorAndMinor()
		{
			local path="$1"
			
			stat -c '%T %t' "$path"
		}
		
		build_needs cat chmod env sh cp
		_libertine_compile_linux_replaceInstallShForIncreasedReproducibility()
		{
			local fileToChange="$inputPath"/arch/"$libertine_hostLinux"/boot/install.sh
			local envShaBang="$(libertine_public_buildNeedsPath)"/env
			{
				printf '%s\n' "$envShaBang"
				cat <<-'EOF'
					main()
					{
						local kernelVersion="$1"
						local kernelImageFile="$2"
						local kernelMapFile="$3"
						local defaultInstallPathOrBlankIfRootDirectory="$4"
						
						cp "$kernelImageFile" "$defaultInstallPathOrBlankIfRootDirectory"/vmlinuz
						cp "$kernelMapFile" "$defaultInstallPathOrBlankIfRootDirectory"/System.map
						printf '%s' "$kernelVersion" >"$defaultInstallPathOrBlankIfRootDirectory"/version
					}
					
					main "$@"
				EOF
				
			} >"$fileToChange"
			
			chmod +x "$fileToChange"
		}
		
		build_needs mv xz pax
		_libertine_compile_linux_copyInitramfs()
		{
			mv "$(libertine_public_inputMakePath linux)"/usr/initramfs_data.cpio.xz "$(libertine_public_outputMiscPath)"/initramfs.cpio.xz
			libertine_public_touchWithKnownDateTime "$(libertine_public_outputMiscPath)"/initramfs.cpio.xz
			
			xz --decompress --stdout "$(libertine_public_outputMiscPath)"/initramfs.cpio.xz | pax -r -w -x ustar -p o -p p | xz --compress -9 --extreme --stdout >"$(libertine_public_outputMiscPath)"/initramfs.tar.xz
			libertine_public_touchWithKnownDateTime "$(libertine_public_outputMiscPath)"/initramfs.tar.xz
		}

		build_needs mkdir cp touch stat dd mkfs.fat rm mount rsync mlbinstall umount
		_libertine_compile_linux_createEfiSystemPartition()
		{
			if [ -z "$libertine_hostEfi" ]; then
				return 0
			fi
			
			# Prepare files on UEFI system partition on regular Linux disk.
				local efiSystemPartitionFolderPath="$(libertine_public_inputMakePath)"/uefi/efi-system-partition
				mkdir -m 0700 -p "$efiSystemPartitionFolderPath"
				pushd "$efiSystemPartitionFolderPath"
			
					mkdir -m 0700 -p EFI/BOOT
					cp "$(libertine_public_outputMiscPath)"/vmlinuz EFI/BOOT/BOOT${libertine_hostEfi}.EFI
					chmod 0500 EFI/BOOT/BOOT${libertine_hostEfi}.EFI
					libertine_public_touchWithKnownDateTime EFI/BOOT/BOOT${libertine_hostEfi}.EFI
					libertine_public_touchWithKnownDateTime EFI/BOOT
					libertine_public_touchWithKnownDateTime EFI
			
					mkdir -m 0700 -p EFI/Microsoft/BOOT
					cp "$(libertine_public_outputMiscPath)"/vmlinuz EFI/Microsoft/BOOT/bootmgfw.efi
					chmod 0500 EFI/Microsoft/BOOT/bootmgfw.efi
					libertine_public_touchWithKnownDateTime EFI/Microsoft/BOOT/bootmgfw.efi
					libertine_public_touchWithKnownDateTime EFI/Microsoft/BOOT
					libertine_public_touchWithKnownDateTime EFI/Microsoft
					libertine_public_touchWithKnownDateTime EFI
			
					printf 'echo -off\r\n%s\r\n' "EFI\BOOT\BOOT${libertine_hostEfi}.EFI" >startup.nsh
					libertine_public_touchWithKnownDateTime startup.nsh
					chmod 0500 startup.nsh

					local rawEfiSizeInBytes="$(stat -c '%s' EFI)"
				popd
			
			# Calculate UEFI system partition size.
				# If using 512-byte clusters...
				if [ $rawEfiSizeInBytes -lt 33549824 ]; then
					rawEfiSizeInBytes=33549824
				fi

				local rawEfiSizeInKilobytes=$(( (rawEfiSizeInBytes + 1023) / 1024))
				local partitionSizeinKilobytes=$(( rawEfiSizeInKilobytes * 32 / 28 ))
			
			# Create an UEFI partition image.
				local vfatPartitionImageOutputFile="$(libertine_public_outputMiscPath)"/libertine-linux.vfat-partition.img
				dd if=/dev/zero of="$vfatPartitionImageOutputFile" bs=1024 count="$partitionSizeinKilobytes"

				local bootMessageFile="$(libertine_public_inputMakePath)"/boot-message-file.txt
				printf 'Invalid Boot Disc\n' >"$bootMessageFile"
				
				# Could use 1st 11 characters of machine name for disk name, and 1st 4 characters for serial id.
				mkfs.fat --invariant -F 32 -f 2 -D 0x80 -M 0xF8 -R 32 -S 512 -n "Libertine" -i ffffffff -m "$bootMessageFile" "$vfatPartitionImageOutputFile"
				libertine_public_touchWithKnownDateTime "$vfatPartitionImageOutputFile"
			
				rm "$bootMessageFile"
			
			# Copy files to partition image and make 
				local mountFolderPath="${destinationFile}".mount
				mkdir -m 0755 -p "$mountFolderPath"
				mount -t vfat -w "$vfatPartitionImageOutputFile" "$mountFolderPath" -o loop,noatime,nodiratime,norelatime,nodev,noexec,nosuid,unbindable,rw,check=strict,conv=binary,tz=UTC,iocharset=iso8859-1,flush
			
				# rsync can not transfer all attributes to a VFAT filesystem.
					set +e
					rsync -a -v "$efiSystemPartitionFolderPath"/ "$mountFolderPath"/
					set -e
				
				# Make bootable
					local linuxCommandLine=''
					mlbinstall "$vfatPartitionImageOutputFile" "$mountFolderPath"/EFI/BOOT/BOOT${libertine_hostEfi}.EFI "$linuxCommandLine"
			
				umount "$mountFolderPath"
				rm -rf "$mountFolderPath"
			
			# Create a bootable ISO-9960 image using either mkisofs in cdrtools or genisofs in crdkit.
				# local isoImageDestinationFile="$(libertine_public_outputMiscPath)"/libertine-linux.iso
				# pushd "$efiSystemPartitionFolderPath"
				# mkisofs -A 'Libertine Linux' -V "$libertine_machine" -volset "$libertine_machine" -J -r -v -x ./lost+found -o "$isoImageDestinationFile" -eltorito-alt-boot -efi-boot "$vfatPartitionImageOutputFile" -no-emul-boot ./
				# popd
			
			# Create a bootable GPT-partitioned USB image
				local vfatGptDiskImageOutputFile="$(libertine_public_outputMiscPath)"/libertine-linux.efi-gpt-disk.img
				local rawGptDiskSizeInBytes="$(stat -c '%s' "$vfatPartitionImageOutputFile")"
				local rawGptDiskSizeInKilobytes=$(( rawFatSizeInBytes * 1024 ))
				local gptDiskSizeInKilobytes=$(( rawGptDiskSizeInKilobytes + 2048 ))
				dd if=/dev/zero of="$vfatGptDiskImageOutputFile" bs=1024 count="$gptDiskSizeInKilobytes"
				sgdisk --new 1:2048:0 --typecode 1:EF00 --mbrtogpt --change-name=1:'Libertine' --partition-guid 1:E6DBA0B7-973D-44B5-8CFA-478A05165684 --disk-guid 1AE17BF1-AE63-4FA0-AAED-16C6EEE8EFB9 "$vfatGptDiskImageOutputFile"
				dd if="$vfatPartitionImageOutputFile" of="$vfatGptDiskImageOutputFile" bs=512 seek=2048 conv=notrunc
				libertine_public_touchWithKnownDateTime "$vfatGptDiskImageOutputFile"
				
			rm -rf "$efiSystemPartitionFolderPath"
		}
		
		build_needs cat touch
		_libertine_compile_linux_createChainloadedIpxeBootScript()
		{
			# This script is suitable as a vultr.com ipxe boot script: https://www.vultr.com/api/#startupscript_create
			cat >"$(libertine_public_outputMiscPath)"/boot.ipxe <<-EOF
				#!ipxe
				
				kernel https://${linux_pxeBootServer}/machines/${libertine_machine}/libertine-linux.vmlinuz
				boot vmlinuz
			EOF
			
			libertine_public_touchWithKnownDateTime "$(libertine_public_outputMiscPath)"/boot.ipxe
		}
		
		build_needs openssl
		_libertine_compile_linux_signLinux()
		{
			libertine_openssl_initializeCertificateAuthority
			
			libertine_openssl_perMachineKernelCodeSigningCertificate
			
			libertine_openssl_codeSign "$(libertine_public_outputMiscPath)"/vmlinuz "$(libertine_public_outputMiscPath)"/vmlinuz.sig
			
			libertine_public_touchWithKnownDateTime "$(libertine_public_outputMiscPath)"/vmlinuz.sig
		}
	;;
	
esac
